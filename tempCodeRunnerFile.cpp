 // if(q < X[0]){
        //     m = -(S_A[N-1])*q + S_AX[N-1];
        // }
        // else if(q > X[N-1]){
        //     m = (S_A[N-1])*q - S_AX[N-1];
        // }
        // else{
        //     m = (S_A[t_idx]*2 - S_A[N-1])*q - (S_AX[t_idx]*2 - S_AX[N-1]);
        // }
        // cout << m << "\n";